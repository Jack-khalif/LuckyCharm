{
  "extends": [
    "next/core-web-vitals",
    "prettier" // Integrates Prettier, disables conflicting rules
  ],
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": "error" // Enforces Prettier as an ESLint rule
  }
}
